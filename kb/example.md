# Example KB doc

This project is a skeleton RAG (retrieval augmented generation) assistant.

It uses:
- FastAPI for the HTTP API
- Celery + Redis for background tasks
- Chroma for a persistent vector store
- Postgres for run storage and traceability
